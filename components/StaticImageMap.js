const StaticMapImage = ({ src }) => {
  return (
    <div className="banner-static-map-image-container">
      <img src={src} className="banner-static-map-image" />
    </div>
  );
};

export default StaticMapImage;
